SELECT * FROM products

  -- //  CRUD
  -- // Create = INSERT
  -- // Read   = SELECT
  -- // Update = UPDATE
  -- // Delete = DELETE

  -- // 'INSERT INTO products (flavor, price, quantity) VALUES ("mint", 1.50, 50);'
  -- // 'SELECT * FROM products'
  -- // 'UPDATE products set price=3.50 WHERE id=4;'
  -- // 'DELETE FROM products WHERE price=3.50;'
